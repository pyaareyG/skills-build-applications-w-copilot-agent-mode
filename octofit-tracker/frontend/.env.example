# Required in GitHub Codespaces so API calls target
# https://${VITE_CODESPACE_NAME}-8000.app.github.dev/api/[component]/
# Copy this file to .env.local and set your Codespace name.
# When unset, the frontend falls back to http://localhost:8000/api/[component]/
VITE_CODESPACE_NAME=
